name: actvity types
on:
  workflow_dispatch:
  pull_request:
    types:
      - opened
      - edited
jobs:
  test-types:
    runs-on: ubuntu-latest
    steps:
     - name: test types
       run: echo "testing types of activity in even github"
